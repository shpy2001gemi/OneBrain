# KU-SEM-001 — Draft meaning: alternatives, references and comparison

> State: Deferred — implemented foundation accepted for this stage under D-024; remaining fidelity work moved to contributor backlog, 2026-09-20.
> Source workspace: `C:/Users/shpy2/Documents/OneBrain`
> Source branch: `codex/ku-web-001-workflow`, substantial uncommitted follow-up work.

## Start here

Latest owner direction: move on to the product framework. Use
[the new-conversation handoff](../NEXT_CONVERSATION.md) for KU-CLI-001 and keep
[remaining semantic work](../outputs/KU_SEM_001_CONTRIBUTOR_BACKLOG.md) deferred.
The implementation/evidence below remains valid; do not resume model tuning or
ask for the same direction approval by following historical instructions below.

Continuation update: owner approved
[the representation proposal](../outputs/KU_SEM_001_REPRESENTATION_PROPOSAL.md).
The active contract is [selection v2](../../../specs/vnext/KU_SEMANTIC_SELECTION_PROFILE_V2.md).
Implementation and development checks are underway; this is not Web activation
or model qualification. The initial handoff below remains historical context.
See [implementation evidence](../outputs/KU_SEM_001_IMPLEMENTATION.md) for the
current checks, raw report names and remaining semantic activation failures.

Read this file, repository AGENTS.md, then only these focused documents:

1. `docs/specs/vnext/KU_SEMANTIC_SELECTION_PROFILE_V1.md`
2. `docs/specs/vnext/KU_REVIEW_DRAFT_PROFILE_V1.md`
3. `docs/handoffs/2026-09-ku-obp-productization/outputs/KU_SEMANTIC_SELECTION_IMPLEMENTATION.md`
4. Relevant SEM/canonical contracts when designing representations; do not load all historical plans.

The owner requested a fresh conversation to avoid context overflow. This handoff
records the proposed next scope; it does not assert that the representation
extension below has already been approved or implemented. Begin with a concise
Vietnamese status and proposed first step; wait for the owner's direction before
implementing that new extension. Prior approval for shared host/LLM division and
headless local development tests remains valid.

## Preserve the actual baseline

The shared sparse-selection implementation is in the SOURCE WORKING TREE, much
of it untracked/uncommitted. A new worktree from the default branch will NOT
contain this implementation. Read the absolute source paths above before deciding
how to prepare an implementation checkout. Never reset/clean the source, replace
it with default-branch files, or mistake missing worktree files for missing work.
Do not commit, push, merge, publish or delete existing work without authorization.

Key code in source workspace:

- `src/ku-encoder/src/extraction/semantic_selection.rs` and `semantic_selection/tests.rs`
- `src/ku-encoder/src/extraction/review_draft.rs` and `review_draft/tests.rs`
- `docs/specs/vnext/ku-semantic-selection-v1/` (shared prompts and schemas)
- `src/onebrain-node/src/ku_product/review_jobs.rs`
- `src/onebrain-api/src/ku_api/tests/experimental_text.rs`
- `src/onebrain-web/src/pages/KuReviewDraft.tsx`
- `scripts/encoder/assess_development_draft.py` and development expectations

## Implemented and tested

Shared Rust owns full-draft assembly, number parsing, quote/evidence anchoring,
relation index resolution and repair limits. LLM emits sparse semantic choices.
Node owns consent, encrypted durable jobs, cancellation and retained revisions.
Web polls background jobs; reads and repeated starts do not resample inference.
New state `draft_extracted` explicitly means unassessed semantics/facts, NOT KU.
No independent verification or canonical lowering is integrated yet.

Latest executor commitment:
`53cf766901d9e84b76902ea1d528d20618372cee9953174e76861db93d10f5c8`.
Do not resume a job under a changed executable commitment silently.

September 20, five KNOWN short development cases (not blind qualification):

| Path | Meaning checks | Mean seconds |
|---|---:|---:|
| Qwen3 8b through real Web API | 5/5 | 52.32 |
| Gemma 4 E4B development chat adapter | 4/5 | 43.19 |
| Gemma 4 12B development chat adapter | 5/5 | 78.58 |

E4B still misses the preposition in the water example. Constrained repair prevents
whole-source argument replacement and excludes self/ambiguous link targets.
15 selection tests, five API tests, 20 Web tests, Web/host build and aggregate
vNext contract validator passed. Five new jobs and three legacy jobs survived
host restart with unchanged call counts and revisions. No KU save/publication.

## New owner-reported counterexample (read from actual stored job)

Exact source:

> Tên lửa có thể sử dụng nhiên liệu lỏng hoặc rắn . Trong đó , nhiên liệu rắn sẽ an toàn hơn.

Operation ID:
`8a0cfcf59373b53e0eb34e3cb721d6ddb2577b0ebe141f4d75d83e11bc3ff0aa`

Qwen3 8b, two calls, charged 84.559 s, state `needs_review`, one retained revision.
It extracted two claims, but:

- Flattened `lỏng hoặc rắn` into two arguments without an explicit alternative.
- Expanded the elided `rắn` to `nhiên liệu rắn`; then grounding matched ambiguous
  occurrences elsewhere instead of representing ellipsis/reference explicitly.
- Added two quantity entries despite the complete absence of numeric quantities.
- Assigned `Trong đó` the relation `cause`; source does not assert causation.
- Dropped `sẽ`.
- Inferred `nhiên liệu lỏng` as comparison target without marking it implicit.
- Evidence spans expanded across clauses to cover the chosen phrases.

Host validation:
`selection[0].anchor: role_quote_absent_or_ambiguous`,
`selection[0].quantities: number_or_annotation_scope`;
uncovered source `hoặc rắn .` and `sẽ`.
The repair only set anchor to the whole source. It did not improve the assembled
draft/validation, so `selection_repair_no_change` stopped further repair.
This is not a timeout. `needs_review` is the correct terminal status; the content
is not a correct completed extraction. No factual claim about rocket safety has
been checked or established.

## Proposed next work (confirm direction in new conversation)

1. Extend the SHARED draft contract to preserve alternatives (`or`), ellipsis /
   cross-clause reference, comparisons and explicit-versus-inferred content.
   Preserve exact source and uncertainty. Resolve compatibility/versioning before
   coding; do not alter canonical KU/SEM silently.
2. Reduce mechanical workload further: disallow numeric-quantity output when no
   numeric syntax exists; host-bound local repair only. Rules must not invent
   semantic relationships or treat absence of digits as proof of no number words.
3. Add this source as development evidence plus broader, separately assessed
   cases for alternatives, references, comparison and temporal/modality scope.
   Do not use or inspect locked VI/EN holdouts. Do not send assessment answers to
   models. Test headlessly and inspect meaning before asking owner to test Web.
4. Independent semantic verification is a separate follow-up after adequate
   representation; canonical lowering and explicit save/share remain later gates.

Owner prioritizes a portable architecture for different user models, not finding
one best model. Keep detailed semantic instructions; remove mechanical work and
redundant output. Keep async jobs and bounded time/call/token/memory budgets.
All platforms consume common Rust/node semantics; do not implement mobile here.

## Local runtime and reproducibility

Runtime root: `C:/Users/shpy2/Documents/OneBrainLocal` (outside repo).
Web: `http://127.0.0.1:4280/ku`; last restart PID 38668, verify current identity.
`host.json`, `start-web.ps1`, `stop-web.ps1`, `README.md`, `provisioning/check_host.py`.
Registry already signed and ready; do not recreate keys/Registry or reset data.
Never print `keys/api-token.txt`, Vault key or signing private key.
Gemma is NOT admitted in Web catalog; only Qwen3 8b. Gemma testing uses the
development-only `review_chat_probe` with installed chat template, `think:false`,
CPU, context 8192, output 2048, seed 1, temperature 0, keep_alive 0.
Qwen production uses admitted raw template and closed thinking prefix.
Ollama 0.34.2 on latest run; September 9 baseline used 0.33.3.
Run models sequentially; dedicated test server is no longer running.
Do not stop the user's default Ollama. Verify PID/path/listener before stopping
owned processes. Windows background services use Hidden windows.

Private evidence in `OneBrainLocal/development-reports`:

- `20260920-selection-v5-web-qwen8b.jsonl`
- `20260920-selection-v5-gemma4-e4b.jsonl`
- `20260920-selection-v5-gemma4-12b.jsonl`
- `20260920-selection-final-assessment.json`
- `20260920-selection-restart-check.json`

Useful commands from repo root:

```powershell
python scripts/ci/validate_vnext_contracts.py
python scripts/encoder/assess_development_draft.py <private-report.jsonl>
python C:/Users/shpy2/Documents/OneBrainLocal/provisioning/check_host.py
```

From `src`: `cargo test -p ku-encoder extraction:: --lib` and
`cargo test -p onebrain-api review_draft_ --lib`.
Host build: `cargo build --locked --config 'profile.dev.package.sha2.opt-level=3' -p onebrain-api --example ku_local_web`.
From `src/onebrain-web`: `npm run test:ku`, `npm run build`.
The framework document is bundle-hashed; if changed, regenerate reviewed encoder
bundle via `python -m scripts.encoder.generate_bundle`, preserve bound LF bytes,
then validate. Avoid unrelated formatting/line-ending churn.
