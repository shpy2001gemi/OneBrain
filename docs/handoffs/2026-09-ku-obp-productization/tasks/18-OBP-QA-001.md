# OBP-QA-001 — Outbound-first desktop product acceptance

> State: Blocked — local preflight passed; independent-network evidence missing
> Branch: `codex/obp-qa-001-nat-canary`
> Depends on: `OBP-CLI-001` and `OBP-DESK-001` merged

## Objective

Latest signed attempt: [V2 happy-case attempt and signed cleanup](../outputs/OBP_QA_001_SIGNED_HAPPY_ATTEMPT_20260923.md).
Four real cross-host probes passed on `85adf65`; the signed three-host session
passed relay matrix and failed closed at reservation. All three hosts finalized
cleanup. A new local durable-sequence correction remains uninstalled. Task 18
is still Blocked pending happy-case, fault and independent consumer evidence.

Latest execution: [installed candidate and successor admission blocker](../outputs/OBP_QA_001_INSTALLED_SUCCESSOR_BLOCKER.md).
All three hosts now bind `2dc5581`, but four real successor probes fail closed.
Relay-b/c are stopped with sequence-2 floors preserved. A versioned verified
history-admission correction is proposed; complete V2 session/fault and product
acceptance remain unexecuted. This supersedes earlier staging-only status.

Latest owner clarification D-036: placement of the existing three VPS on three
separate physical machines is already supplied and accepted. Reuse the retained
placement evidence without requiring reacquisition, keeping its original
provenance and provider-document-pending limitation. Earlier fresh-placement
blockers are superseded; remaining technical admission and execution gates stand.

Prove the bounded product claims for two ordinary consumer nodes using
outbound-only relay connectivity, independent relay operation, bootstrap
source loss, relay failover, restart and privacy controls.

## Required read set

- `../README.md`, `../DECISIONS.md`, `../CAPABILITY_STATUS.md`, `../PROGRESS.md`
- accepted OBP product, API, CLI, Web and Desktop contracts/evidence
- outbound-first acceptance sections and current P5/production evidence rules
- existing NAT, failover, reconciliation, privacy and restart harnesses

## Deliverable

Versioned acceptance scenarios and evidence for:

- two consumer application nodes with no inbound port-forwarding;
- two independently configured vNext `onebrain-relay` instances;
- initial bootstrap by DNS or direct IP plus expected NodeID;
- learned approved sources after initial bootstrap outage;
- selected-relay loss and alternate-relay recovery;
- bidirectional application exchange when either side initiates intent;
- process restart, durable outbox and reconciliation convergence;
- malicious/incorrect relay, wrong-peer and private-data negative cases;
- consistent CLI, Web and Desktop status.

## Acceptance

- Single-machine simulation is labeled preflight, not independent-network
  production evidence.
- Product claims require the exact host/network/provider evidence prescribed
  by the accepted profile.
- Passing evidence supports Desktop/reference scope only; not mobile, browser,
  mailbox wake, guaranteed availability or global discovery.
- The feature remains default-off after this task.
- All prescribed test and evidence validators pass.

## Excluded

Default rollout, strict Base qualification, mobile/browser and legacy removal.

## Current local work — 2026-09-23

[Versioned scenarios and evidence lanes](../outputs/OBP_QA_001_ACCEPTANCE_V1.md)
map local harnesses to the independent-network acceptance oracles. The collector
runs local preflight only and cannot qualify a product/platform. Task acceptance
still requires the missing real host/network evidence; no live-host operation
or P5 production dispatch is authorized by this local work.

Measured results: [local preflight evidence](../outputs/OBP_QA_001_PREFLIGHT.md).
All local groups pass within their explicit skip/fixture limits; this is not task
acceptance or platform qualification.

## Authorized P5 follow-up — 2026-09-23

The owner subsequently authorized SSH upload and remote execution using the
existing three hosts. [Execution record](../outputs/OBP_QA_001_P5_UPLOAD.md):
all three uploads and artifact checks passed; six single-host native preflight
runs passed. This does not satisfy independent-network acceptance. The exact
V2 authority was [recovered](../outputs/OBP_QA_001_RECOVERED_P5_AUTHORITY.md), but
its policy expired and old requests bind a different candidate. Renewal and
fresh bindings remain required. The earlier local-only restriction above is
historical for this scoped follow-up. No installed service was changed.

The owner has since approved the renewal; fresh signed Base request and Registry
binding are verified. [Latest blocker](../outputs/OBP_QA_001_P5_RENEWAL_BLOCKER.md):
the two live descriptors expired and `fc65f08` lacks state-preserving renewal.
Bundle 02 passed on all hosts, but no qualifying P5 session can be formed until
the runtime correction and a new immutable candidate are available.

## Continuation scope — relay lifecycle defect correction

The requested continuation addresses the concrete renewal defect recorded above
under D-002, within the existing relay lifecycle and canonical successor rules.
Local work covers `onebrain-relay` runtime/state, regression tests and operator
instructions. The existing `export-candidate-descriptor` command uses explicit
configured sequence: first export is sequence 1; a successor is exactly current
sequence + 1, same key and signed configuration, with the stored predecessor's
canonical digest. Equal sequence only republishes the exact stored bytes after
checking configuration, never re-signs or extends expiry. Gaps/rollback reject.

The durable commit precedes publication, atomically fencing old activation.
Publication is create-new and atomic; a failed publication leaves the committed
candidate recoverable by equal-sequence export to a new path. Renewal preserves
all other replay/control/nonce/reservation/revocation/rendezvous records. Fresh
digest-bound probes are required before activation; no new CLI, wire object,
product endpoint, planner, outbox or provisioning authority is introduced.

This local correction is reviewable work, not an immutable qualification
candidate. Git publication remains outside the current authorization. Rebuild
and exact-candidate authority binding remain required before remote use. Task 18
stays Blocked on qualification evidence and task 19 stays unstarted.

[Correction, verification and next gate](../outputs/OBP_QA_001_RELAY_RENEWAL_FIX.md)
records the completed local implementation. This does not complete task 18.

D-035 owner acceptance and [candidate `2dc5581` execution](../outputs/OBP_QA_001_CANDIDATE_2DC5581.md)
complete commit/rebuild/staging preparation. Fresh Base/Registry bindings and
all three artifact checks pass; six native preflights remain nonqualifying.
Current topology/clock and signed V2 admission gates remain open.
